# NOTAS:

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --num1     es la base de la tabla de multiplicar       [number] [required]
  -l, --listar   listar tabla en consola              [boolean] [default: false]
  -h, --hasta    hasta donde llega la tabla                  [number] [required]
```